---
'@halolib-ui/angular': minor
---

Add the `ha-breadcrumb` component (tokens, icon support, and an overlay-based
collapse for long trails), extend `ha-icon` with `strokeWidth`,
`nonScalingStroke`, and `title` inputs, and give `ha-select`'s trigger a
rotating chevron icon whose click now toggles the panel open/closed instead of
only opening it.
